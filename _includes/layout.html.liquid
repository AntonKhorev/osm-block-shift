<!DOCTYPE html>
<html lang="ru">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Сдвиг квартала</title>
	<style>{% include "node_modules/simpledotcss/simple.min.css" %}</style>
</head>
<body>
	{% assign previousChapter = collections.chapters | getPreviousCollectionItem %}
	{% assign nextChapter = collections.chapters | getNextCollectionItem %}
	<header>
		{% if previousChapter or nextChapter %}
			<nav>
				{% if previousChapter %}
					<a href="{{ previousChapter.url }}">&lt; {{ previousChapter.data.title }}</a>
				{% endif %}
					<a href="/">Оглавление</a>
				{% if nextChapter %}
					<a href="{{ nextChapter.url }}">{{ nextChapter.data.title }} &gt;</a>
				{% endif %}
			</nav>
		{% else %}
			<nav>
				<a href="https://github.com/AntonKhorev/osm-block-shift">GitHub</a>
			</nav>
		{% endif %}
	</header>
	<h1>{{ title | default: "Сдвиг квартала" }}</h1>
	<main>
		{{ content }}
	</main>
	{% if previousChapter or nextChapter %}
		<header>
			<nav>
				{% if previousChapter %}
					<a href="{{ previousChapter.url }}">&lt; {{ previousChapter.data.title }}</a>
				{% endif %}
					<a href="/">Оглавление</a>
				{% if nextChapter %}
					<a href="{{ nextChapter.url }}">{{ nextChapter.data.title }} &gt;</a>
				{% endif %}
			</nav>
		</header>
	{% endif %}
</body>
</html>
